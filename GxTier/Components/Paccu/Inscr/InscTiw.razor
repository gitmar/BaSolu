@page "/insctiw"
@layout EnrolMenu

@using System
@using System.Collections.ObjectModel
@using System.ComponentModel.DataAnnotations
@using System.Dynamic
@using System.Net.Http.Headers
@using System.Security.Claims
@using System.Reflection
@using System.Text
@using System.Text.Json
@using System.Web
@using BlazorBootstrap
@using GxShared.Sess
@using GxShared.Others
@using GxShared.Helpers
@using GxTie.ClieModels
@using GxTie.Services
@using GxTie.Components.Paccu.RtPer
@* @using GxTie.Services.Mform *@
@using GxWapi.DaModels
@using Microsoft.AspNetCore.Components.Forms
@using Microsoft.AspNetCore.WebUtilities
@using Microsoft.OData.Client
@using Newtonsoft.Json

@inject NavigationManager _navmanager
@inject HttpClientService _cliemanager
@inject RendAgres _rendAgres

<PageTitle>RECEPTION</PageTitle>
<div>
    @Imessage
</div>
@if (isLoading)
{
    <Progress class="progress mb-3">
        <ProgressBar Width="100" Label="Persos/Accueil Loading..." Type="ProgressType.StripedAndAnimated" Color="ProgressColor.Primary" />
    </Progress>
}
<!--downmodal-->
@if (curTiw != null && isModalVisible == true)
{
    <GlobModal IsVisible="@isModalVisible"
               IsVisibleChanged="@OnModalVisibilityChanged"
               Title="Chargement des documents"
               EntyTiw="@curTiw"
               EntyTie="null"
               LsmVars="@LsVars"
               LsmFixes="@LsFixes"
               OnTiwSave="HandleTiwSave"
               OnTieSave="HandleTieSave"
               Oparent="2">
    </GlobModal>
}
    <div class="my-container">
        @if (toChild != null)
        {
            @ChildContent
        }
    </div>
    @if (!isLoading)
    {
        @if (curTiw != null && edTiwContext != null)
        {
        <AuthorizeView>
                <Authorized Context="authtiwcontext">
                    <EditForm EditContext="edTiwContext" @key="curTiw">
                    <DataAnnotationsValidator />
                    <ValidationSummary />
                        <div class="container">
                            <Tabs>
                                <Tab Title="Candidats">
                                    <Content>
                                        <div class="row">
                                            <!--Tiewel contents-->
                                            <div class="col-md-6">
                                            <div class="d-flex align-items-center gap-2">
                                                <Button Color="ButtonColor.Primary"
                                                        Size="ButtonSize.Small"
                                                        Class="px-2 py-1 small-btn"
                                                        Disabled="@(!AllowMultipleEdits && isAddTiw)"
                                                        @onclick="StartTiwNew">
                                                    ➕ Add
                                                </Button>

                                                @if (isAddTiw)
                                                {
                                                    <Button Color="ButtonColor.Info"
                                                            Size="ButtonSize.Small"
                                                            Class="px-2 py-1 small-btn"
                                                            @onclick="ShowModal">
                                                        piece(s)
                                                    </Button>

                                                    <Button Color="ButtonColor.Success"
                                                            Size="ButtonSize.Small"
                                                            Class="px-2 py-1 small-btn"
                                                            @onclick="() => ConfirmEdit(curTiw, EntityLevel.Plan)">
                                                        ✅ Confirm
                                                    </Button>

                                                    <Button Color="ButtonColor.Secondary"
                                                            Size="ButtonSize.Small"
                                                            Class="px-2 py-1 small-btn"
                                                            @onclick="() => CancelEdit(curTiw, EntityLevel.Plan)">
                                                        ❌ Cancel
                                                    </Button>
                                                }

                                            </div>
                                                @if(draftTiw != null)
                                                {
                                            <Grid TItem="Tiewel"
                                                  Data="@MyDaTiws.Where(ut => ut.Xadd1 == -1).ToList()"
                                                  Responsive="true">
                                                <GridColumns>
                                                    <GridColumn TItem="Tiewel" HeaderText="">
                                                        <!-- 🔥 HEADER: Direct child of GridColumn -->
                                                        <ChildContent Context="selectedTiw">
                                                            <!-- 🔥 VERTICAL FORM - Each label+value = 1 ROW -->
                                                            <!-- FIELD 1: ID (Read-only) -->
                                                            <div style="display: flex; align-items: center; gap: 1rem;">
                                                                <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">ID:</label>
                                                                <div class="form-control-plaintext col-md-9">@selectedTiw.Id</div>
                                                            </div>
                                                            <!-- FIELD 2: Dossier -->
                                                            @if (TiwVisibility.GetValueOrDefault("dossier", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Dossier:</label>
                                                                    @if (EdTiwRowguid == draftTiw?.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftTiw.Xmatri" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Xmatri</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 3: Nom -->
                                                            @if (TiwVisibility.GetValueOrDefault("nom", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nom:</label>
                                                                    @if (EdTiwRowguid == draftTiw?.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftTiw.Nom" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Nom</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 4: Pnom -->
                                                            @if (TiwVisibility.GetValueOrDefault("pnom", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Pnom:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftTiw.Pnom" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Pnom</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 5: Nomb / de jeune fille -->
                                                            @if (TiwVisibility.GetValueOrDefault("bnom", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nom de Jeune fille:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftTiw.Bnom" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Bnom</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 6: Statut -->
                                                            @if (TiwVisibility.GetValueOrDefault("statut", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Statut:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Statut">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 1 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Statut" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 1 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 7: Relat -->
                                                            @if (TiwVisibility.GetValueOrDefault("relat", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Relat:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Relat">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 8 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Relat" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 8 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 8: Sexe -->
                                                            @if (TiwVisibility.GetValueOrDefault("sexe", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Sexe:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Sexe">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 2 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Sexe" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 2 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 9: Sitmat -->
                                                            @if (TiwVisibility.GetValueOrDefault("sitmat", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Sitmat:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Sitmat">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 3 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Sitmat" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 3 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 10: fonction -->
                                                            @if (TiwVisibility.GetValueOrDefault("ifonc", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Fonction:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Ifonc">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 4 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Ifonc" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 4 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 11: grade -->
                                                            @if (TiwVisibility.GetValueOrDefault("igrad", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Grade:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Igrad">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 5 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Igrad" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 5 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 12: usremail -->
                                                            @if (TiwVisibility.GetValueOrDefault("email", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Email:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftTiw.Email" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Email</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 13: Phone -->
                                                            @if (TiwVisibility.GetValueOrDefault("phone", true))
                                                            {
                                                                <div style="display: flex; a13lign-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Phone:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftTiw.Phone" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Phone</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 14: Dnais -->
                                                            @if (TiwVisibility.GetValueOrDefault("dnais", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Date nais.:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputDate @bind-Value="draftTiw.Dnais" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Dnais</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 15: Demb -->
                                                            @if (TiwVisibility.GetValueOrDefault("demb", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Date emb.:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputDate @bind-Value="draftTiw.Demb" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Demb</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 16: Nenf -->
                                                            @if (TiwVisibility.GetValueOrDefault("nenf", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nb enf.:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputNumber @bind-Value="draftTiw.Nenf" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedTiw.Nenf</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 17: resp. -->
                                                            @if (TiwVisibility.GetValueOrDefault("respon", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Responsabilite:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Respon">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 7 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Respon" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 7 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 18: resid/pays. -->
                                                            @if (TiwVisibility.GetValueOrDefault("rpays", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">P.residence:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Rpays">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Abg">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Rpays" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Abg">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 19: natio/pays. -->
                                                            @if (TiwVisibility.GetValueOrDefault("natio", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nationalite:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Natio">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Abg">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Natio" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 20: methP. -->
                                                            @if (TiwVisibility.GetValueOrDefault("ipaid", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">M.Paiement:</label>
                                                                    @if (EdTiwRowguid == draftTiw.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftTiw.Ipaid">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 14 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedTiw.Ipaid" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 14 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                        </ChildContent>
                                                    </GridColumn>
                                                </GridColumns>
                                            </Grid>
                                                }    
                                            </div>
                                            <!--Tiwafl contents-->
                                            <div class="col-md-6">
                                                @if(draftRub != null)
                                                {
                                            <Grid TItem="Tiwafl"
                                                  Data="@curTiw.Tiwafls.Where(tf => tf.Xadd1 == -1).ToList()"
                                                  AllowPaging="true"
                                                  Responsive="true">
                                                <GridColumns>
                                                    <GridColumn TItem="Tiwafl" HeaderText="">
                                                        <ChildContent Context="selectedAfl">
                                                            <!-- 🔥 VERTICAL FORM - Each label+value = 1 ROW -->
                                                                <div class="d-flex align-items-center gap-2">
                                                                   @if (curTiw != null && curTiw.Xedt1 == -1)
                                                                   {
                                                                        <label>Gpe Afil :</label>
                                                                        <InputSelect id="gpId" @bind-Value="IMyGAfl">
                                                                            <option value="0">g.afl?</option>
                                                                            @foreach (var afl in LsGAfls)
                                                                            {
                                                                                <option value="@afl.Elea" @onclick="SelDomAfil">@afl.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                        <Button Color="ButtonColor.Primary"
                                                                                Size="ButtonSize.Small"
                                                                                Class="px-2 py-1 small-btn"
                                                                                Disabled="@(!AllowMultipleEdits && isAddFmt)"
                                                                                @onclick="() => StartRubNew(curTiw, 1)">
                                                                            ➕ Add
                                                                        </Button>
                                                                    }
                                                                    @switch (GetRowState(selectedAfl.Rowguid))
                                                                                {
                                                                                    case RowState.Locked:
                                                                                        <span class="text-muted">—</span>
                                                                                        break;
                                                                                    case RowState.DeletePending:
                                                                                        <Button Color="ButtonColor.Danger" 
                                                                                            Size="ButtonSize.Small"
                                                                                            Class="px-2 py-1 small-btn"
                                                                                                @onclick="() => ConfirmDelete(selectedAfl, EntityLevel.Rub)">
                                                                                            ⚠️ Confirm
                                                                                        </Button>
                                                                                        <Button Color="ButtonColor.Secondary" 
                                                                                            Size="ButtonSize.Small"
                                                                                            Class="px-2 py-1 small-btn"
                                                                                                @onclick="() => CancelDelete(selectedAfl, EntityLevel.Rub)">
                                                                                            ❎ Cancel
                                                                                        </Button>
                                                                                        break;
                                                                                    case RowState.EditPending:
                                                                                        <Button Color="ButtonColor.Success" 
                                                                                            Size="ButtonSize.Small"
                                                                                            Class="px-2 py-1 small-btn"
                                                                                                @onclick="() => ConfirmEdit(selectedAfl, EntityLevel.Rub)">
                                                                                            ✅ Confirm
                                                                                        </Button>
                                                                                        <Button Color="ButtonColor.Secondary" 
                                                                                            Size="ButtonSize.Small"
                                                                                            Class="px-2 py-1 small-btn"
                                                                                                @onclick="() => CancelEdit(selectedAfl, EntityLevel.Rub)">
                                                                                            ❌ Cancel
                                                                                        </Button>
                                                                                        break;

                                                                                    default:
                                                                                        <Button Color="ButtonColor.Warning" 
                                                                                            Size="ButtonSize.Small"
                                                                                            Class="px-2 py-1 small-btn"
                                                                                                @onclick="() => StartRubEdit(selectedAfl, selectedAfl.Rowguid)">
                                                                                            ✏️ Edit
                                                                                        </Button>
                                                                                        <Button Color="ButtonColor.Danger" 
                                                                                            Size="ButtonSize.Small"
                                                                                    Class="px-2 py-1 small-btn"
                                                                                                @onclick="() => StartRowDelete(selectedAfl.Rowguid, EntityLevel.Rub)">
                                                                                            🗑 Delete
                                                                                        </Button>
                                                                                        break;
                                                                                }
                                                            </div>
                                                            <!-- FIELD 1: ID (Read-only) -->
                                                            <div style="display: flex; align-items: center; gap: 1rem;">
                                                                <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">ID:</label>
                                                                <div class="form-control-plaintext col-md-9">@selectedAfl.Id</div>
                                                            </div>
                                                            <!-- FIELD 2: Dossier -->
                                                            @if (AflVisibility.GetValueOrDefault("dossier", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Dossier:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftRub.Xmatri" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Xmatri</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 3: Nom -->
                                                            @if (AflVisibility.GetValueOrDefault("nom", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nom:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftRub.Nom" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Nom</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 4: Pnom -->
                                                            @if (AflVisibility.GetValueOrDefault("pnom", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Pnom:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftRub.Pnom" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Pnom</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 5: Nomb / de jeune fille -->
                                                            @if (AflVisibility.GetValueOrDefault("bnom", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nom de Jeune fille:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftRub.Bnom" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Bnom</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 6: Statut -->
                                                            @if (AflVisibility.GetValueOrDefault("statut", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Statut:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Statut">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 1 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Statut" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 1 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 7: Relat -->
                                                            @if (AflVisibility.GetValueOrDefault("relat", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Relat:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Relat">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 8 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Relat" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 8 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 8: Sexe -->
                                                            @if (AflVisibility.GetValueOrDefault("sexe", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Sexe:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Sexe">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 2 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Sexe" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 2 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 9: Sitmat -->
                                                            @if (AflVisibility.GetValueOrDefault("sitmat", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Sitmat:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Sitmat">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 3 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Sitmat" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 3 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 10: fonction -->
                                                            @if (AflVisibility.GetValueOrDefault("ifonc", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Fonction:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Ifonc">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 4 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Ifonc" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 4 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 11: grade -->
                                                            @if (AflVisibility.GetValueOrDefault("igrad", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Grade:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Igrad">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 5 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Igrad" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 5 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 12: usremail -->
                                                            @if (AflVisibility.GetValueOrDefault("email", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Email:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftRub.Email" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Email</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 13: Phone -->
                                                            @if (AflVisibility.GetValueOrDefault("phone", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Phone:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputText @bind-Value="draftRub.Phone" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Phone</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 14: Dnais -->
                                                            @if (AflVisibility.GetValueOrDefault("dnais", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Date nais.:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputDate @bind-Value="draftRub.Dnais" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Dnais</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 15: Demb -->
                                                            @if (AflVisibility.GetValueOrDefault("demb", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Date emb.:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputDate @bind-Value="draftRub.Demb" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Demb</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 16: Nenf -->
                                                            @if (AflVisibility.GetValueOrDefault("nenf", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nb enf.:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputNumber @bind-Value="draftRub.Nenf" class="form-control flex-grow-1" style="min-width: 200px;" />
                                                                    }
                                                                    else
                                                                    {
                                                                        <div class="form-control-plaintext flex-grow-1">@selectedAfl.Nenf</div>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 17: resp. -->
                                                            @if (AflVisibility.GetValueOrDefault("respon", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Responsabilite:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Respon">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 7 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Respon" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 7 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 18: resid/pays. -->
                                                            @if (AflVisibility.GetValueOrDefault("rpays", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">P.residence:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Rpays">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Abg">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Rpays" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Abg">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 19: natio/pays. -->
                                                            @if (AflVisibility.GetValueOrDefault("natio", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">Nationalite:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Natio">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Abg">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Natio" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 31 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                            <!-- FIELD 20: methP. -->
                                                            @if (AflVisibility.GetValueOrDefault("ipaid", true))
                                                            {
                                                                <div style="display: flex; align-items: center; gap: 1rem;">
                                                                    <label class="fw-bold col-form-label col-md-3 text-nowrap" style="min-width: 120px;">M.Paiement:</label>
                                                                    @if (EdRubRowguid == draftRub.Rowguid)
                                                                    {
                                                                        <InputSelect @bind-Value="draftRub.Ipaid">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 14 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                    else
                                                                    {
                                                                        <InputSelect @bind-Value="selectedAfl.Ipaid" disabled="true">
                                                                            <option class="jchx" value="0">?choisir</option>
                                                                            @foreach (var tob in TbElems.Where(ut => ut.Pitb == 14 && ut.Elea != 0))
                                                                            {
                                                                                <option value="@tob.Elea">@tob.Liba</option>
                                                                            }
                                                                        </InputSelect>
                                                                    }
                                                                </div>
                                                            }
                                                        </ChildContent>
                                                    </GridColumn>
                                                </GridColumns>
                                            </Grid>
                                                }
                                            </div>
                                        </div>
                                    </Content>
                                </Tab>
                                <Tab Title="Verification">
                                    <Content>

                                    </Content>
                                </Tab>
                            </Tabs>
                        </div>
                        <!--En bas ordinaire-->
                        <div class="container row">
                        <Grid TItem="Tiewel"
                              Data="@MyDaTiws.Where(tu => tu.Xadd1 != -1).ToList()"
                              @key="TiwRenderKey"
                              AllowDetailView="true"
                              AllowPaging="true"
                              Responsive="true">
                            <GridColumns>
                                <GridColumn TItem="Tiewel" HeaderText="No">
                                    <ChildContent Context="item">
                                        <span>@(item.Xedt1 == -1 ? item?.Iui : (item?.Iui == 0 ? item?.Iele : item?.Iui))</span>
                                    </ChildContent>
                                </GridColumn>
                                <GridColumn TItem="Tiewel" HeaderText="Dossier">
                                    <ChildContent Context="item">
                                        @item.Xmatri
                                    </ChildContent>
                                </GridColumn>
                                <GridColumn TItem="Tiewel" HeaderText="Nom">
                                    <ChildContent Context="item">
                                        @item.Nom
                                    </ChildContent>
                                </GridColumn>
                                <GridColumn TItem="Tiewel" HeaderText="Pnom">
                                    <ChildContent Context="item">
                                        @item.Pnom
                                    </ChildContent>
                                </GridColumn>
                                <GridColumn TItem="Tiewel" HeaderText="etat" IsVisible="true">
                                    <ChildContent Context="item">
                                            <InputSelect @bind-Value="item.Eta" disabled="true">
                                                <option class="jchx" value="0">?choisir</option>
                                                @foreach (var tob in LsFixes.Where(ut => ut.Gvars == 32 && ut.Itb == 1 && ut.Elea != 0))
                                                {
                                                    <option value="@tob.Elea">@tob.Liba</option>
                                                }
                                            </InputSelect>
                                    </ChildContent>
                                </GridColumn>
                                <GridColumn TItem="Tiewel" HeaderText="Actions" Filterable="false" Class="xcol2 actions-col">
                                    <ChildContent Context="item">
                                        @switch (GetRowState(item.Rowguid))
                                        {
                                            case RowState.Locked:
                                                <span class="text-muted">—</span>
                                                break;

                                            case RowState.DeletePending:
                                                <Button Color="ButtonColor.Danger" Size="ButtonSize.Small"
                                                        @onclick="() => ConfirmDelete(item, EntityLevel.Plan)">
                                                    ⚠️ Confirm
                                                </Button>
                                                <Button Color="ButtonColor.Secondary" Size="ButtonSize.Small"
                                                        @onclick="() => CancelDelete(item, EntityLevel.Plan)">
                                                    ❎ Cancel
                                                </Button>
                                                break;
                                            case RowState.EditPending:
                                                <Button Color="ButtonColor.Success" Size="ButtonSize.Small"
                                                        @onclick="() => ConfirmEdit(item, EntityLevel.Plan)">
                                                    ✅ Confirm
                                                </Button>
                                                <Button Color="ButtonColor.Secondary" Size="ButtonSize.Small"
                                                        @onclick="() => CancelEdit(item, EntityLevel.Plan)">
                                                    ❌ Cancel
                                                </Button>
                                                break;
                                            default:
                                                <Button Color="ButtonColor.Warning" Size="ButtonSize.Small"
                                                        @onclick="() => StartManTiwEdit(item, 1)">
                                                    ✏️ Edit
                                                </Button>
                                                <Button Color="ButtonColor.Danger" Size="ButtonSize.Small"
                                                        @onclick="() => StartRowDelete(item.Rowguid, EntityLevel.Plan)">
                                                    🗑 Delete
                                                </Button>
                                                break;
                                        }
                                    </ChildContent>
                                </GridColumn>
                            </GridColumns>
                        </Grid>
                        </div>
                </EditForm>
            </Authorized>
        </AuthorizeView>
        } else {
            <div class="text-center p-3">
                <p>No records available.</p>
                @* <button class="btn btn-primary" @onclick="() => InitializeData()">
                    Initialize Data
                </button> *@
            </div>
        }
    }
<style>
    .inputfl {
        opacity: 0;
        width: 0.1;
        height: 0.1;
        position: absolute;
    }

    .disabled {
        pointer-events: none; /* Prevents clicking */
        opacity: 0.5; /*visual cue for disabled state */
    }

    .gpe-select {
        font-size: 24px;
        font-weight: bold;
    }

        .gpe-select option {
            font-size: 18px;
            font-weight: bold;
        }

    .my-container {
        background-color: yellow;
    }

    .maxh-400 {
        max-height: 400px;
        overflow-y: auto;
    }

    .disabled-action {
        pointer-events: none;
        opacity: 0.5;
    }

    .grid-container {
        display: grid;
        grid-template-columns: max-content minmax(max-content, auto);
        gap: 8px 16px;
    }

    .grid-row {
        display: contents; /* makes children participate directly in the grid */
    }

    .grid-container label {
        grid-column: 1;
        font-weight: bold;
        white-space: nowrap;  /* prevent label wrap */
    }

    .grid-container > div {
        grid-column: 2;
    }

    .standard-width {
        width: 200px; /* or any fixed width like 150px, 100%, etc */
        max-width: 100%;
    }

    .fs-xs {
        font-size: 0.65rem;
    }
    .small-btn {
        font-size: 0.75rem; /* smaller text */
        line-height: 1.2;
    }
</style>

@code {
    private bool isLoading = true;
    private string Imessage = "", Jmessage = "", Kmessage = "";
    private bool yaPgLink = false;

    [CascadingParameter]
    private Task<AuthenticationState>? AuthState { get; set; }
    [Parameter]
    public RenderFragment ChildContent { get; set; }
    [Parameter]
    public MyODataContext curODContext { get; set; }
    [Parameter]
    public Gxorga curOrga { get; set; } = new();
    [Parameter]
    public MyShareVars toChild { get; set; }
    [Parameter]
    public int? Tgpe { get; set; }
    [Parameter]
    public int? Ugpe { get; set; }
    [Parameter]
    public int toMenu { get; set; }
    //guard
    [Parameter]
    public PendingChangesGuard _guard { get; set; }
    [Parameter]
    public EventCallback<PendingState> OnPendingStateChanged { get; set; }
    [Parameter]
    public int NbChanges { get; set; }
    [Parameter]
    public EventCallback<int> NbChangesChanged { get; set; }  // 🔥 CRITICAL

    private EditContext? edTiwContext;
    //private EditContext? edAflContext;
    //author
    private ClaimsPrincipal authUser;
    private bool isAuthenticated = false;
    //usrbag
    private Userbag _usrBag = new Userbag();
    private string _UserId = "";
    private int _UorgId = 0;

    private Dictionary<int, bool> EdState = new Dictionary<int, bool>();
    private Dictionary<Guid, RowState> _rowStates = new();
    private Dictionary<Guid, bool> _isLightBg = new();  // Track light background
    private Dictionary<string, bool> TiwVisibility = new();
    private Dictionary<string, bool> AflVisibility = new();

    private void SetLightBackground(Guid rowguid, bool isLight)
    {
        _isLightBg[rowguid] = isLight;
    }
    private bool GetLightBackground(Guid rowguid)
    {
        return _isLightBg.GetValueOrDefault(rowguid, false);
    }
    // Disable ALL actions when ANY row editing
    private Guid? _activeEditRowguid = null;
    private EntityLevel? _activeEditLevel = null;
    private bool CanEditRows() => _activeEditRowguid == null;
    private bool IsRowEditing(Guid rowguid) => _activeEditRowguid == rowguid;
    private bool IsAnyRowEditing => _activeEditRowguid != null;

    public enum RowState
    {
        Normal,
        EditPending,
        DeletePending,
        AddPending,  // 🔥 NEW for Add
        Locked
    }
    public enum EntityLevel { Plan, Rub, Fmt }

    private List<Gplan> LsPlans = new();
    private List<Gpfixe> LsFixes = new();
    private List<Gpfixe> LsGAfls = new();
    public List<Gtabl> TbGpes = new();
    public List<Gtvar> TbElems = new();
    //public List<Gpvar> TbCols = new();
    private List<Gpvar> LsVars = new();
    private List<Gpfixe> LsDoms = new();
    private List<Gpfixe> LsAtrs = new();
    private List<Gpfixe> LsVues = new();

    private bool isPlnFilteringEnabled = false;
    private bool isRubFilteringEnabled = false;

    //Global
    private int _lgcdu = 0;
    private int IMyGpln = 5; //5Programme
    private int IMyDom = 1; //1Saisie
    private int IMyVue = 1; //1Reel2Virtuel
    private int IMyGAfl = 0; //1cn2par3enf4pai5flh
    private string atrTitle = "";

    private string atrDest = "Programmes de Saisie";
    //pour modals formulas
    private Tiewel curTiw = new();
    private Tiwafl curRubr = new();
    private Rubfmt curFmat = new();

    private bool isRubCodeValid = false; //child
    ////private string DisabledClass => !EdtDelVue ? "disabled" : "";
    /// </summary> <summary>
    ///
    //modals
    private bool isModalVisible = false;
    //data
    private ObservableCollection<Tiewel> MyDaTiws { get; set; } = new();
    //private List<Tiewel> LsPlsEchs => curOrga.Tiewels.Where(pl => pl.Ptyp == 2).ToList();
    //private List<Tiewel> LsPlsGris => curOrga.Tiewels.Where(pl => pl.Ptyp == 3).ToList();
    //private List<Tiewel> LsPlsDivs => curOrga.Tiewels.Where(pl => pl.Ptyp == 4).ToList();
    //add/edit options
    private bool AllowMultipleEdits { get; set; } = false;
    private bool isFiltre { get; set; } = false;
    //TABS
    private string ActiveTab { get; set; } = "Candidat";

    //PLAN
    private Guid? TiwRenderKey = null;
    private Dictionary<Guid, Tiewel> _edPlnOriginals = new();
    private Tiewel? draftTiw = null;
    private bool isAddTiw = false;
    private bool isEdtTiw = false;
    private Guid? AdTiwRowguid = null;
    private Guid? EdTiwRowguid = null;// Currently editing row ID
    private Guid? DlTiwRowguid = null;
    //RUBRIQUE
    private Guid? RubRenderKey = null;
    private Dictionary<Guid, Tiwafl> _edRubOriginals = new();
    private Tiwafl? draftRub = null;
    private bool isAddRub = false;
    private bool isEdtRub = false;
    private Guid? AdRubRowguid = null;
    private Guid? EdRubRowguid = null;// Currently editing row ID
    private Guid? DlRubRowguid = null;
    //FORMAT
    private Guid? FmtRenderKey = null;
    private Dictionary<Guid, Rubfmt> _edFmtOriginals = new();
    private Rubfmt? draftFmt = null;
    private bool isAddFmt = false;
    private bool isEdtFmt = false;
    //private bool isDelFmt = false;
    private Guid? AdFmtRowguid = null;
    private Guid? EdFmtRowguid = null;// Currently editing row ID
    private Guid? DlFmtRowguid = null;

    bool IsTiwRowLocked(Guid rowGuid) =>
        !AllowMultipleEdits &&
        (EdTiwRowguid.HasValue || DlTiwRowguid.HasValue) &&
        EdTiwRowguid != rowGuid &&
        DlTiwRowguid != rowGuid;
    bool IsRubRowLocked(Guid rowGuid) =>
        !AllowMultipleEdits &&
        (EdRubRowguid.HasValue || DlRubRowguid.HasValue) &&
        EdRubRowguid != rowGuid &&
        DlRubRowguid != rowGuid;
    //Modals formule
    Modal pForuModal = new(); //plans
    Modal rForuModal = new(); //rubriques
    Modal gForuModal = new(); //formats
    private string patTitle = "", chlTitle = "", gsnTitle = "";
    private string patInitTitle = "", chlInitTitle = "", gsnInitTitle = "";
    private string pstrFormu = "", rstrFormu = "", fstrFormu = "";

    protected override async Task OnInitializedAsync()
    {
        isLoading = true;
        try
        {
            if (toChild == null || curOrga == null)
            {
                Console.WriteLine("Missing parent data");
                return;
            }
            isAuthenticated = toChild.isAuthenticated;
            authUser = toChild.authUser;
            if (!isAuthenticated)
            {
                Imessage = "CONNEXION, Utilisateur non authentifie";
                return;
            }
            LsVars = toChild.LsVars;
            LsFixes = toChild.LsFixes;
            LsGAfls = toChild.LsFixes.Where(ua => ua.Gvars == 53 && ua.Itb == 6 && ua.Elea != 0).ToList();
            //TbElems = toChild.TbElems;
            _lgcdu = 3;
            var noslims = LsVars.Where(uv => uv.Idhie == 0 && uv.Gvars == 21 && uv.Itb == 1 && uv.Elea == 1);
            if (noslims.Any()) _lgcdu = noslims.FirstOrDefault().Ivala;
            LsVues = LsFixes.Where(ut => ut.Gvars == 36 && ut.Itb == 4 && ut.Gp1 == 5 && ut.Elea != 0).ToList();
            //chargement des tables;
            var datbls = await _rendAgres.LoadOrgaTables();
            TbGpes = datbls?.Hdtbls ?? new List<Gtabl>();
            TbElems = datbls?.Tbelems ?? new List<Gtvar>();
            Console.WriteLine($"nb Table elements {TbElems.Count}");
            LsPlans = toChild.LsPlans;
            //LsSites = toChild.LsSites;

            //ChargeParentData;
            Console.WriteLine($"Child init: Tiewels={curOrga.Tiewels?.Count ?? 0}");
            // 🔥 SAFE - Only initialize if Tiewels EXISTS
            //editcontext Model initialising
            curTiw ??= new();  // Or load data here
            if (curOrga.Tiewels != null && curOrga.Tiewels.Any())
            {
                Console.WriteLine($"Tiewels loaded: {curOrga.Tiewels.Count}");

                MyDaTiws = new ObservableCollection<Tiewel>(
                    curOrga.Tiewels
                    .ToList());
                if (MyDaTiws.Any())
                {
                    curTiw = MyDaTiws.FirstOrDefault();
                }
            }
            else
            {
                Console.WriteLine("Tiewels null/empty - initializing empty collections");
                MyDaTiws = new ObservableCollection<Tiewel>();
            }
            //Dictionnaires de visibilite
            TiwVisibility = new();
            AflVisibility = new();
            var LsAcols = LsVars.Where(v => v.Gvars == 8 && v.Itb == 1 && v.Totyp == Ugpe && v.Elea != 0).ToList();
            if (LsAcols.Any())
            {
                string StrStore = LsAcols.FirstOrDefault().Fgsrc; //ColName;IsVisible;
                List<LSelCol> Pcolumns = ColsPreferences.DeserializePreferences(StrStore);
                TiwVisibility = Pcolumns.Where(pc => pc.EtRef == "1").ToDictionary(tc => tc.ColName, tc => tc.IsVisible);
            }
            atrTitle = "Saisie";
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Child crash: {ex.Message}");
            Console.WriteLine($"Stack: {ex.StackTrace}");  // 🔥 More debug info
            Imessage = $"Erreur chargement: {ex.Message}";
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }
    protected override void OnParametersSet()
    {
        if (curTiw != null)
        {
            edTiwContext ??= new EditContext(curTiw);
            // Or always recreate: edTiwContext = new EditContext(curTiw);
        }
        // if (curRubr != null)
        // {
        //     edAflContext ??= new EditContext(curRubr);
        //     // Or always recreate: edTiwContext = new EditContext(curTiw);
        // }
    }

    private async Task StartTiwNew()
    {
        Console.WriteLine("🚨 StartPlnNew");
        var lsnb = NextSeq(MyDaTiws, p => p.Iui);
        var newTiw = new Tiewel
        { /* init properties */
            Rowguid = Guid.NewGuid(),
            Idorg = curOrga.Idorg, //pere Id
            Iui = lsnb, //nextSeq(1),//Rowguid = Guid.NewGuid(),
            Utyp = Ugpe,
            Nom = "Martin ESSEC",
            Pnom = "Simpore",
            Datc = DateTime.Now,
            Eta = 1,
            Xadd1 = -1,
            Xedt1 = 0,
            Xdel1 = 0
        };
        SetRowState(newTiw.Rowguid, RowState.AddPending);
        SetLightBackground(newTiw.Rowguid, true);
        MyDaTiws.Insert(0, newTiw);
        draftTiw = (Tiewel)DeepClone(newTiw);
        EdTiwRowguid = draftTiw.Rowguid;
        // curTiw = newTiw;
        isAddTiw = true;
        isEdtTiw = true;
        SetRowState(draftTiw.Rowguid, RowState.EditPending);  // Shows Confirm/Cancel ✓
        TiwRenderKey = draftTiw.Rowguid;
        Console.WriteLine($"✅ New row added to curOrga.Tiewels - Count: {curOrga.Tiewels.Count}");
        curTiw = newTiw;
        StartRowEdit(draftTiw.Rowguid, EntityLevel.Plan);
        Console.WriteLine("99🚨Issue");
        //StateHasChanged();
    }
    private async Task StartTiwEdit(Tiewel item, Guid rowguid)
    {
        var isNew = StaticMethods.IsNew(curODContext, item, e => e.Id);
        if (isNew)
        { //not saved yet
            Imessage = "🚫 Cannot edit new Plan: Please save it first.";
            return;
        }
        if (rowguid == Guid.Empty) return;
        item.Xedt1 = -1;
        if (item.Iele != 0) item.Iui = (int)item.Iele;
        SetRowState(rowguid, RowState.EditPending);  // Shows Confirm/Cancel buttons
        _edPlnOriginals[rowguid] = (Tiewel)DeepClone(item);
        EdTiwRowguid = AllowMultipleEdits ? null : rowguid;
        draftTiw = (Tiewel)DeepClone(item);
        isEdtTiw = true;
        StartRowEdit(rowguid, EntityLevel.Plan);
        //StateHasChanged();
    }
    private async Task StartRubNew(Tiewel mytiw, int mytst)
    {
        if (mytiw.Id <= 0)
        {
            Imessage = "🚫 Cannot add Affilie: Candidat must be saved first.";
            return;
        }
        Console.WriteLine("🚨 StartRubNew");
        var lsnb = NextSeq(mytiw.Tiwafls, p => p.Iui);
        var newRub = new Tiwafl
        { /* init */

            Rowguid = Guid.NewGuid(),
            Itie = curTiw.Id,
            Ttyp = Ugpe,
            Tyafl = mytst,
            Irang = lsnb,
            Nom = "Fils Martin",
            Pnom = "Ilboudo",
            Datc = DateTime.Now,
            Liba = "",
            Abg = "",
            Eta = 1,
            Xadd1 = -1, //newly added/edited
            Xedt1 = 0,
            Xdel1 = 0
        };
        SetRowState(newRub.Rowguid, RowState.AddPending);
        SetLightBackground(newRub.Rowguid, true);
        if (mytiw.Tiwafls == null)
        {
            mytiw.Tiwafls = new DataServiceCollection<Tiwafl>();  // ✅ Correct type
        }
        mytiw.Tiwafls.Insert(0, newRub);
        draftRub = (Tiwafl)DeepClone(newRub);
        EdRubRowguid = draftRub.Rowguid;
        isAddRub = true;
        isEdtRub = true;
        SetRowState(draftRub.Rowguid, RowState.EditPending);  // 🔥 ADD
        RubRenderKey = draftRub.Rowguid;  // Force grid refresh
        Console.WriteLine($"✅ New row added to Tiewel.Tiwafls - Count: {mytiw.Tiwafls.Count}");
        StartRowEdit(draftRub.Rowguid, EntityLevel.Rub);
        //StateHasChanged();
    }
    private async Task StartManTiwEdit(Tiewel mytiw, int mytst)
    {
        curTiw = mytiw;

    }
    private async Task StartRubEdit(Tiwafl item, Guid rowguid)
    {
        var isNew = StaticMethods.IsNew(curODContext, item, e => e.Id);
        if (isNew)
        { //not saved yet
            Imessage = "🚫 Cannot edit new Rubrique: Please save it first.";
            return;
        }
        if (rowguid == Guid.Empty) return;

        item.Xedt1 = -1;
        if (item.Iele != 0) item.Iui = (int)item.Iele;
        SetRowState(rowguid, RowState.EditPending);  // Shows Confirm/Cancel buttons
        _edRubOriginals[rowguid] = (Tiwafl)DeepClone(item);
        EdRubRowguid = AllowMultipleEdits ? null : rowguid;
        draftRub = (Tiwafl)DeepClone(item);
        isEdtRub = true;
        StartRowEdit(rowguid, EntityLevel.Rub);
        //StateHasChanged();
    }
    // ✅ 2.1 CONFIRM OPERATIONS
    private async Task ConfirmPlnEdit(Tiewel item) =>
        await UnifiedEditAction(item, true, EntityLevel.Plan);
    private async Task ConfirmRubEdit(Tiwafl item) =>
        await UnifiedEditAction(item, true, EntityLevel.Rub);
    // ✅ 2.2 CANCEL OPERATIONS
    private async Task CancelPlnEdit(Tiewel item) =>
        await UnifiedEditAction(item, false, EntityLevel.Plan);
    private async Task CancelRubEdit(Tiwafl item) =>
        await UnifiedEditAction(item, false, EntityLevel.Rub);

    private async Task UnifiedEditAction(object item, bool isConfirm, EntityLevel level)
    {
        switch (level)
        {
            case EntityLevel.Plan:
                await ProcessPlan((Tiewel)item, isConfirm);
                break;
            case EntityLevel.Rub:
                await ProcessRub((Tiwafl)item, isConfirm);
                break;
                // case EntityLevel.Rub:
                //     await ProcessFmt((Rubfmt)item, isConfirm);
                //     break;
        }
    }

    private async Task ProcessPlan(Tiewel pln, bool isConfirm)
    {
        await ProcessEntity(pln, isConfirm, "Tiewels", p => ValidatePln(p));
    }
    private async Task ProcessRub(Tiwafl rub, bool isConfirm)
    {
        await ProcessEntity(rub, isConfirm, "Tiwafls", r => ValidateRub(r));
    }

    private async Task ProcessEntity<T>(T entity, bool isConfirm, string entitySet, Func<T, bool> validate)
    where T : class
    {
        Console.WriteLine($"🔄 Process{(typeof(T).Name)}: {(isConfirm ? "CONFIRM" : "CANCEL")}");
        if (entity == null)
        {
            Imessage = "entity nulle";
            StateHasChanged();
            return;
        }
        if (isConfirm)
        {
            CopyDraftToGridItem<T>(entity);
            // 🔥 1. VALIDATE FIRST - uses your ValidateXXX() helpers
            if (!validate(entity))
            {
                // Imessage already set by ValidatePln/ValidateRub/ValidateFmt
                Console.WriteLine($"❌ Validation failed: {Imessage}");
                StateHasChanged();
                return;
            }
            // 🔥 2. NEW vs EXISTING LOGIC
            bool isNewAdd = GetXadd1(entity) == -1;
            if (isNewAdd)
            {
                // 🔥 NEW ADD: AddObject (not tracked yet)
                curODContext.AddObject(entitySet, entity);
                Console.WriteLine($"➕ NEW {typeof(T).Name} → AddObject");
            }
            else
            {
                // 🔥 EXISTING: Attach + UpdateObject
                if (!curODContext.IsEntityTracked(entity))
                {
                    curODContext.Context.AttachTo(entitySet, entity);
                    Console.WriteLine($"📥 Attached existing {entitySet}");
                }
                curODContext.Context.UpdateObject(entity);
                Console.WriteLine($"✏️ EXISTING {typeof(T).Name} → UpdateObject");
            }
            // 🔥 2. Container: Attach → Update → Flags
            if (!curODContext.IsEntityTracked(entity))
            {
                curODContext.Context.AttachTo(entitySet, entity);
                Console.WriteLine($"📥 Attached {entitySet}");
            }

            SetFlags(entity, 0, -1, 0);  // Confirmed: Xadd1=0, Xedt1=-1
            curODContext.Context.UpdateObject(entity);

            Console.WriteLine($"✅ {typeof(T).Name} confirmed → tracked");

            // 🔥 3. UI cleanup
            ResetRowState((Guid)typeof(T).GetProperty("Rowguid")!.GetValue(entity)!);
            SetLightBackground((Guid)typeof(T).GetProperty("Rowguid")!.GetValue(entity)!, false);
            ResetEntityUIState<T>();
            await NotifyPendingChanges();
            Imessage = $"✅ {typeof(T).Name} confirmed";
        }
        else  // CANCEL
        {
            if (GetXadd1(entity) == -1)  // New add
            {
                if (curODContext.IsEntityTracked(entity))
                    curODContext.Context.Detach(entity);
                RemoveFromParentCollection(entity);
                Console.WriteLine($"🗑️ New {typeof(T).Name} canceled");
            }
            else
            {
                ClearDraft<T>();
            }
            ResetRowState((Guid)typeof(T).GetProperty("Rowguid")!.GetValue(entity)!);
            ResetEntityUIState<T>();
            Imessage = "";
        }

        StateHasChanged();
    }

    // Generic flag setter
    private void SetFlags<T>(T entity, int xadd1, int xedt1, int xdel1)
    {
        typeof(T).GetProperty("Xadd1")?.SetValue(entity, xadd1);
        typeof(T).GetProperty("Xedt1")?.SetValue(entity, xedt1);
        typeof(T).GetProperty("Xdel1")?.SetValue(entity, xdel1);
    }
    // private void ResetFlags(object item)
    // {
    //     SetFlags(item, 0, 0, 0);  // Xadd1=0, Xedt1=0, Xdel1=0 (normal state)
    // }
    // Generic Xadd1 getter
    private int GetXadd1<T>(T entity)
    {
        return (int)typeof(T).GetProperty("Xadd1")?.GetValue(entity)!;
    }
    // Remove from parent collection
    private void RemoveFromParentCollection<T>(T entity)
    {
        // Plngen: top-level
        if (entity is Tiewel pln && MyDaTiws.Any(p => p.Rowguid == pln.Rowguid))
            MyDaTiws.Remove(pln);
        // Rubvar: parent Plngen.Rubvars
        else if (entity is Tiwafl rub)
        {
            var parent = MyDaTiws.FirstOrDefault(p => p.Id == rub.Itie);
            parent?.Tiwafls?.Remove(rub);
        }
    }
    private void CopyDraftToGridItem<T>(T gridItem) where T : class
    {
        switch (typeof(T).Name)
        { // ONLY editable UI fields - 5-10x faster than reflection!
            case nameof(Tiewel):
                if (draftTiw == null) return;
                var pln = (Tiewel)(object)gridItem;
                pln.Smatri = draftTiw.Smatri; //designation
                pln.Xmatri = draftTiw.Xmatri; //abrege
                pln.Nom = draftTiw.Nom; //formule-non valider
                pln.Pnom = draftTiw.Pnom;
                pln.Raison = draftTiw.Raison;
                pln.Eta = draftTiw.Eta; //etat
                break;
            case nameof(Tiwafl):
                if (draftRub == null) return;
                var rub = (Tiwafl)(object)gridItem;
                rub.Irang = draftRub.Irang;
                rub.Smatri = draftRub.Smatri; //designation
                rub.Xmatri = draftRub.Xmatri; //abrege
                rub.Nom = draftRub.Nom; //formule-non valider
                rub.Pnom = draftRub.Pnom;
                rub.Raison = draftRub.Raison;
                rub.Eta = draftRub.Eta; //etat
                break;
            // case nameof(Rubfmt):
            //     if (draftFmt == null) return;
            //     var fmt = (Rubfmt)(object)gridItem;
            //     fmt.Liba = draftFmt.Liba; //designation
            //     fmt.Abg = draftFmt.Abg; //abrege
            //     fmt.Zcdrub = draftFmt.Zcdrub; //code
            //     fmt.Xcod = draftFmt.Xcod; //refer
            //     fmt.Ztyp = draftFmt.Ztyp; //ztyp
            //     fmt.Ftsrc = draftFmt.Ftsrc; //formule
            //     fmt.Col = draftFmt.Col; //col
            //     fmt.Lne = draftFmt.Lne; //lne
            //     fmt.Lgtf = draftFmt.Lgtf; //lgtf
            //     fmt.Eta = draftFmt.Eta; //etat
            //     break;
        }
    }
    private void ClearDraft<T>()
    {
        switch (typeof(T).Name)
        {
            case nameof(Tiewel):
                draftTiw = null;
                Console.WriteLine("EdTiwRowguid is become null");
                EdTiwRowguid = null;
                break;
            case nameof(Tiwafl):
                draftRub = null;
                EdRubRowguid = null;
                break;
            case nameof(Rubfmt):
                draftFmt = null;
                EdFmtRowguid = null;
                break;
        }
    }
    // ROWS MANAGEMENT
    private void StartRowEdit(Guid rowguid, EntityLevel level)
    {
        _activeEditRowguid = rowguid;
        _activeEditLevel = level;
        StateHasChanged();
    }
    private void EndRowEdit()
    {
        _activeEditRowguid = null;
        _activeEditLevel = null;
        StateHasChanged();
    }
    private string GetTiwRowClass(Tiewel pln)
    {
        var state = GetRowState(pln.Rowguid);
        var isLight = IsLightBackground(pln.Rowguid);  // 🔥 Uses your existing

        // 🔥 NEW: Single Active Row Editing
        var isActiveEdit = _activeEditRowguid == pln.Rowguid;
        var isOtherRowEditing = _activeEditRowguid != null && !isActiveEdit;

        return GetRowCss(state, isLight, pln.Rowguid, isActiveEdit, isOtherRowEditing);
    }
    private string GetRubRowClass(Tiwafl rub)
    {
        var state = GetRowState(rub.Rowguid);
        var isLight = IsLightBackground(rub.Rowguid);  // 🔥 Uses your existing

        // 🔥 NEW: Single Active Row Editing
        var isActiveEdit = _activeEditRowguid == rub.Rowguid;
        var isOtherRowEditing = _activeEditRowguid != null && !isActiveEdit;

        return GetRowCss(state, isLight, rub.Rowguid, isActiveEdit, isOtherRowEditing);
    }
    private string GetFmtRowClass(Rubfmt fmt)
    {
        var state = GetRowState(fmt.Rowguid);
        var isLight = IsLightBackground(fmt.Rowguid);  // 🔥 Uses your existing

        // 🔥 NEW: Single Active Row Editing
        var isActiveEdit = _activeEditRowguid == fmt.Rowguid;
        var isOtherRowEditing = _activeEditRowguid != null && !isActiveEdit;

        return GetRowCss(state, isLight, fmt.Rowguid, isActiveEdit, isOtherRowEditing);
    }
    private RowState GetRowState(Guid rowguid, object? parent = null)
    {
        if (_rowStates.TryGetValue(rowguid, out var state)) return state;

        // Context-aware lookup
        if (parent is Tiewel mytiw)
        {
            if (mytiw.Rowguid == rowguid && IsTiwRowLocked(rowguid)) return RowState.Locked;
            var rub = mytiw.Tiwafls?.FirstOrDefault(r => r.Rowguid == rowguid);
            if (rub != null && IsRubRowLocked(rowguid)) return RowState.Locked;
        }
        // else if (parent is Tiwafl myrub)
        // {
        //     var fmt = myrub.Rubfmts?.FirstOrDefault(f => f.Rowguid == rowguid);
        //     if (fmt != null && IsFmtRowLocked(rowguid)) return RowState.Locked;
        // }

        // Full hierarchy fallback
        // ... same as above
        //Dictionary lookup by RowGuid
        // 🔥 MISSING: Final return for normal state
        return _rowStates.TryGetValue(rowguid, out var cachedState) ? cachedState : RowState.Normal;
    }
    private void SetRowState(Guid rowguid, RowState state)  // Remove <T>
    {
        _rowStates[rowguid] = state;
    }
    private void ResetRowState(Guid rowguid)  // Reset specific row
    {
        _rowStates.Remove(rowguid);  // Back to Normal
    }
    private bool IsLightBackground(Guid rowguid)
    {
        return _isLightBg.ContainsKey(rowguid) && _isLightBg[rowguid];
    }

    private void ResetEntityUIState<T>()
    {
        switch (typeof(T).Name)
        {
            case nameof(Tiewel): ResetPlnUI(); break;
            case nameof(Tiwafl): ResetRubUI(); break;
            //case nameof(Rubfmt): ResetFmtUI(); break;
        }
    }
    private void ResetPlnUI()
    {
        Console.WriteLine("🔄 ResetPlnUI");

        // 🔥 CLEAR ALL MODES
        isEdtTiw = false;
        isAddTiw = false;  // ← CRITICAL - Exits add mode

        draftTiw = null;
        EdTiwRowguid = null;
        DlTiwRowguid = null;

        TiwRenderKey = Guid.NewGuid();  // Force grid refresh
        StateHasChanged();
    }
    private void ResetRubUI()
    {
        draftRub = null; 
        EdRubRowguid = AdRubRowguid = null;
        isEdtRub = isAddRub = false; 
        RubRenderKey = null;
    }
    // private void ResetFmtUI()
    // {
    //     draftFmt = null; EdFmtRowguid = AdFmtRowguid = null;
    //     isEdtFmt = isAddFmt = false; FmtRenderKey = null;
    // }
    // ✅ 3. NOTIFICATIONS
    private async Task NotifyPendingChanges()
    {
        await OnPendingStateChanged.InvokeAsync(new PendingState(
            _guard.HasPendingChanges(),
            _guard.GetPendingChangesCount()
        ));

        NbChanges = _guard.GetPendingChangesCount();
        await NbChangesChanged.InvokeAsync(NbChanges);
        StateHasChanged();
    }

    private void StartDelete<T>(T item, string entitySet) where T : class
    {
        var rowguid = ((dynamic)item).Rowguid;
        SetDeleteFlags(item); // e.g. Xdel1 = -1
        SetRowState(rowguid, RowState.DeletePending);  // Triggers
        Console.WriteLine($"🗑 {typeof(T).Name} {rowguid} marked as pending delete");
        StateHasChanged();
    }
    private void StartRowDelete(Guid rowguid, EntityLevel level)
    {
        // 🔥 1. Set DeletePending → triggers your @switch
        SetRowState(rowguid, RowState.DeletePending);

        // 🔥 2. Enable single-row editing (CSS)
        StartRowEdit(rowguid, level);

        StateHasChanged();
    }
    private async Task ConfirmDelete<T>(T item, EntityLevel level) where T : class
    {
        await UnifiedDeleteAction(item, true, level);
        EndRowEdit();  // Close single delete mode
    }
    // SINGLE method - ALL delete cancels
    private async Task CancelDelete<T>(T item, EntityLevel level) where T : class
    {
        await UnifiedDeleteAction(item, false, level);
        EndRowEdit();  // Close single delete mode
    }

    private void SetDeleteFlags(object item) => SetFlags(item, 0, 0, -1);

    // SINGLE method handles ALL confirm operations
    private async Task ConfirmEdit<T>(T item, EntityLevel level) where T : class
    {
        await UnifiedEditAction(item, true, level);
        EndRowEdit();  // Close single edit mode
    }
    // SINGLE method handles ALL cancel operations
    private async Task CancelEdit<T>(T item, EntityLevel level) where T : class
    {
        await UnifiedEditAction(item, false, level);
        EndRowEdit();  // Close single edit mode
    }
    private async Task UnifiedDeleteAction<T>(T item, bool isConfirm, EntityLevel level) where T : class
    {
        switch (level)
        {
            case EntityLevel.Plan: await ProcessDelete<Tiewel>(item as Tiewel, isConfirm); break;
            case EntityLevel.Rub: await ProcessDelete<Tiwafl>(item as Tiwafl, isConfirm); break;
            //case EntityLevel.Rub: await ProcessDelete<Rubfmt>(item as Rubfmt, isConfirm); break;
        }
    }
    private async Task ProcessDelete<T>(T item, bool isConfirm) where T : class
    {
        if (item == null) return;

        var rowguid = (Guid)typeof(T).GetProperty("Rowguid")!.GetValue(item)!;

        if (isConfirm)
        {
            // 🔥 Generic delete logic
            RemoveFromParentCollection(item);

            if (!curODContext.IsEntityTracked(item))
                curODContext.Context.AttachTo(GetEntitySetName<T>(), item);

            curODContext.Context.DeleteObject(item);
            SetDeleteFlags(item);
            SetRowState(rowguid, RowState.Normal);
            await NotifyPendingChanges();
        }
        else
        {
            SetRowState(rowguid, RowState.Normal);
        }

        StateHasChanged();
    }
    private string GetEntitySetName<T>() where T : class
    {
        return typeof(T) switch
        {
            Type t when t == typeof(Tiewel) => "Tiewels",
            Type t when t == typeof(Tiwafl) => "Tiwafls",
            //Type t when t == typeof(Rubfmt) => "Rubfmts",
            _ => throw new ArgumentException($"Unknown entity type: {typeof(T).Name}")
        };
    }
    private string GetEntitySet(object entity)
    {
        return entity switch
        {
            Tiewel _ => "Tiewels",
            Tiwafl _ => "Tiwafls",
            //Rubfmt _ => "Rubfmts",
            _ => throw new ArgumentException("Unknown entity type")
        };
    }

    // ✅ 6. VALIDATIONS
    private bool ValidateEntity<T>(T item)
    {
        return item switch
        {
            Tiewel pln => ValidatePln(pln),
            Tiwafl rub => ValidateRub(rub),
            //Rubfmt fmt => ValidateFmt(fmt),
            _ => true
        };
    }
    private bool ValidatePln(Tiewel? mytiw)
    {
        Console.WriteLine($"A verifier {mytiw.Nom}");
        //liba
        if (string.IsNullOrEmpty(mytiw.Nom)
            || mytiw.Nom.Length < 4)
        {
            Imessage = "designation obligatoire/longueur";
            return false;
        }
        //abrege
        if (string.IsNullOrEmpty(mytiw.Pnom)
            || mytiw.Pnom.Length < 4)
        {
            Imessage = "abrege obligatoire/longueur";
            return false;
        }
        // //etat
        // if (mytiw.Eta < 0)
        // {
        //     Imessage = "etat obligatoire";
        //     return false;
        // }
        return true;
    }
    private bool ValidateRub(Tiwafl myrub)
    {
        //liba
        if (string.IsNullOrEmpty(myrub.Nom)
            || myrub.Nom.Length < 4)
        {
            Imessage = "designation obligatoire/longueur";
            return false;
        }
        //abrege
        if (string.IsNullOrEmpty(myrub.Pnom)
            || myrub.Pnom.Length < 4)
        {
            Imessage = "abrege obligatoire/longueur";
            return false;
        }
        //scdrub
        // if (string.IsNullOrEmpty(myrub.Scdrub)
        //     || myrub.Scdrub.Length != _lgcdu)
        // {
        //     Imessage = "code obligatoire/longueur";
        //     return false;
        // }
        // //etat
        // if (myrub.Eta < 0)
        // {
        //     Imessage = "etat obligatoire";
        //     return false;
        // }
        return true;
    }
    // ✅ 6. ROW Management
    private string GetRowCss(RowState state, bool isLight, Guid rowguid,
                        bool isActiveEdit, bool isOtherRowEditing)
    {
        var css = new List<string>();

        if (isLight) css.Add("bg-light");

        // 🔥 YOUR EXISTING: NEW ADD detection (Xadd1 == -1)
        var isNewAdd = ((dynamic)MyDaTiws.FirstOrDefault(p => p.Rowguid == rowguid))?.Xadd1 == -1;

        // 🔥 HIGHEST PRIORITY: Single Row Editing
        if (isActiveEdit)
        {
            css.Add("row-active-edit");  // Thick yellow border + highlight
            return string.Join(" ", css);  // EARLY RETURN - editing overrides all
        }

        // 🔥 NEXT: Other rows when editing active → READONLY
        if (isOtherRowEditing)
        {
            css.Add("row-readonly");
            css.Add("opacity-65");
            css.Add("cursor-not-allowed");
        }

        // 🔥 YOUR EXISTING RowState logic
        switch (state)
        {
            case RowState.AddPending:
            case RowState.EditPending when isNewAdd:
                css.Add("border-start border-warning border-3");
                //css.Add("new-add-indicator");
                break;
            case RowState.EditPending:
                css.Add("border-start border-info border-3");
                break;
            case RowState.DeletePending:
                css.Add("bg-danger-subtle");
                break;
            case RowState.Locked:
                css.Add("bg-secondary-subtle opacity-75");
                break;
        }

        return string.Join(" ", css);
    }
    // ✅ 6. SELECTIONS
    private void SelPlanVue(int xdom)
    {
        if (xdom != 0)
        {

        }
    }
    private void SelDomAfil()
    {
        AflVisibility = new();
        if (Ugpe == 1)
        {
            var LsAcols = LsVars.Where(v => v.Gvars == 8 && v.Itb == 1 && v.Totyp == 1 && v.Etyp == IMyGAfl && v.Elea != 0).ToList();
            if (LsAcols.Any())
            {
                string StrStore = LsAcols.FirstOrDefault().Fgsrc; //ColName;IsVisible;
                List<LSelCol> Pcolumns = ColsPreferences.DeserializePreferences(StrStore);
                AflVisibility = Pcolumns.Where(pc => pc.EtRef == "1").ToDictionary(tc => tc.ColName, tc => tc.IsVisible);
            }
         }
    }
    // private void SelPlanDom(int xdom)
    // {
    //     if (xdom != 0)
    //     {

    //     }
    // }

    // ✅ 6. HELPERS - Mark Count and Clone

    private void MarkAsModified(object entity)
    {
        // Reset flags universally
        if (entity is Tiewel pln) { pln.Xedt1 = pln.Xadd1 = pln.Xdel1 = 0; }
        else if (entity is Tiwafl rub) { rub.Xedt1 = rub.Xadd1 = rub.Xdel1 = 0; }
        else if (entity is Rubfmt fmt) { fmt.Xedt1 = fmt.Xadd1 = fmt.Xdel1 = 0; }

        try { curODContext.Context.AttachTo(GetEntitySet(entity), entity); }
        catch (InvalidOperationException) { }  // Already tracked

        curODContext.Context.UpdateObject(entity);  // 🔥 KEY: Context.UpdateObject()
        Console.WriteLine($"✅ {entity.GetType().Name} marked Modified");
    }
    private object DeepClone(object entity)
    {
        var settings = new JsonSerializerSettings
        {
            ReferenceLoopHandling = ReferenceLoopHandling.Ignore
        };

        var json = JsonConvert.SerializeObject(entity, settings);
        return JsonConvert.DeserializeObject(json, entity.GetType(), settings)!;
    }
    bool AreRowsEqual(Tiwafl row1, Tiwafl row2) =>
        row1.Liba == row2.Liba && row1.Abg == row2.Abg;
    private int NextSeq<T>(IEnumerable<T> source, Func<T, int?> selector)
    {
        int maxSeq = source.Any() ? source.Max(selector) ?? 0 : 0;
        return maxSeq + 1;
    }
    //FILTRES
    private Grid<Tiewel> grid = default!;
    private async void TogglePlnFiltering()
    {
        isPlnFilteringEnabled = !isPlnFilteringEnabled;
        await grid.RefreshDataAsync(); // Refresh the grid to apply the change
    }
    private void ToggleRubFiltering()
    {
        isRubFilteringEnabled = !isRubFilteringEnabled;
    }

    //PLAN Formule modals
    void showFrPlnModal(Tiewel mytiw, int orig)
    {
        pstrFormu = "";
        // if (!string.IsNullOrEmpty(mytiw.Fpsrc))
        // {
        //     //pstrFormu = mytiw.Liba.Substring(0, 8);
        // }

        curTiw = mytiw;

        if (pForuModal != null)
        {
            pForuModal.BodyCssClass = (orig == 1) ? "editable" : "readonly";
            pForuModal.ShowAsync();
        }
    }
    //RUBR Formule modals
    void showFrRubModal(Tiwafl myrub, int orig)
    {
        rstrFormu = "";
        // if (!string.IsNullOrEmpty(myrub.Frsrc))
        // {
        //     rstrFormu = myrub.Liba.Substring(0, 8);
        // }

        curRubr = myrub;

        if (rForuModal != null)
        {
            rForuModal.BodyCssClass = (orig == 1) ? "editable" : "readonly";
            rForuModal.ShowAsync();
        }
    }
    
    private void ShowModal()
    {
        // Optionally, set the current entity to edit
        //currentEntity = new YourEntityType { Name = "Sample Name", Description = "Sample Description" }; // Example initialization
        //_mdcTiwel = curTiwel;
        Console.WriteLine($"ShowModal appele");
        isModalVisible = true;
    }
    private Task OnModalVisibilityChanged(bool isVisible)
    {
        isModalVisible = isVisible;
        return Task.CompletedTask;
    }
    private Task HandleTiwSave(Tiewel entity)
    {
        // Handle modal entity save
        // For example, save to the database or update the UI
        Console.WriteLine($"Entity saved: {entity.Nom}, {entity.Pnom}");
        return Task.CompletedTask;
    }
    private Task HandleTieSave(Tiersp entity)
    {
        // Handle modal entity save
        // For example, save to the database or update the UI
        Console.WriteLine($"Entity saved: {entity.Nom}, {entity.Pnom}");
        return Task.CompletedTask;
    }
    private void showPgLink()
    {
        yaPgLink = !yaPgLink;
    }
    private string GetCodeStyle()
    {
        return "width:50%;";
    }
    private string GetStyle(int? itemId)
    {
        int oId = Convert.ToInt32(itemId);
        Console.WriteLine($" style state : {oId}");
        return EdState.TryGetValue(oId, out bool onEditing) && onEditing
        ? "background-color:yellow;"
        : "";
    }
    private string GetButton(int itemId)
    {
        int oId = Convert.ToInt32(itemId);
        return EdState.TryGetValue(oId, out bool onEditing) && onEditing ? "Confirm" : "Edit";
    }

    // private void OnChlBlurHandler(Tiewel mypl, Tiwafl myvar)
    // {
    //     Console.WriteLine("ATTENTION : En Blur....");
    //     ////isRubCodeValid = HandleChlInput(mypl, myvar);
    //     Imessage = $"OnBlur active : {isRubCodeValid}";
    //     // Optionally trigger UI refresh or logging
    // }
    private void OnChlFocusHandler(Tiewel mypl, Tiwafl myvar)
    {

    }
    // private bool HandleChlInput(Tiewel mypl, Tiwafl myvar)
    // {
    //     var input = myvar.Smatri?.Trim();
    //     Jmessage = "";
    //     Console.WriteLine($"code entre traiter {input}");
    //     // Basic null/empty check
    //     if (string.IsNullOrEmpty(input))
    //     {
    //         Jmessage = "Le code est requis.";
    //         return false;
    //     }
    //     // Explicit length check
    //     if (input.Length != _lgcdu)
    //     {
    //         Jmessage = $"Le code doit contenir exactement {_lgcdu} chiffres.";
    //         return false;
    //     }
    //     // Digit-only check/No de poste peut ne pas etre numerique
    //     if (!Regex.IsMatch(input, @"^[0-9]+$")) // @"^[a-zA-Z0-9_\-\/\\|{}]+$"))
    //     {
    //         //(!Regex.IsMatch(input, @"^\d+$")) /numeric
    //         //(Regex.IsMatch(input, @"^[a-zA-Z0-9]+$")) /alphanumeric strict
    //         //(Regex.IsMatch(input, @"^[a-zA-Z0-9_\-\/\\|{}]+$") /add some others characters
    //         //(Regex.IsMatch(input, @"^[a-zA-Z0-9_\-\/\\|{}]{6}$")) //limit number to 6
    //         //(Regex.IsMatch(input, @"^[a-zA-Z0-9_\-\/\\|{}]{4,10}$")) //limit a range 4-10
    //         Jmessage = "Le code ne doit contenir que des chiffres.";
    //         return false;
    //     }
    //     // Uniqueness check
    //     var exists = mypl.Tiwafls?.Any(uva => uva.Scdrub == input) ?? false;
    //     if (exists)
    //     {
    //         Jmessage = "Ce code rubrique existe déjà.";
    //         return false;
    //     }
    //     return true;
    // }
    private void HandleFieldChanged(object sender, FieldChangedEventArgs e)
    {
        //ChildUPDING = true;
        //if (!isTiwInEditing) isTiwInEditing = true;
        Console.WriteLine($"Field edited: {e.FieldIdentifier.FieldName}");
        // Place your logic here when a field is edited
    }
    private void ResetEditContext(Tiewel model)
    {
        // Dispose old handlers before replacing
        if (edTiwContext != null)
        {
            edTiwContext.OnFieldChanged -= HandleFieldChanged;
        }

        edTiwContext = new EditContext(model);
        edTiwContext.OnFieldChanged += HandleFieldChanged;
    }
    public void Dispose()  // ← ADD THIS
    {
        Console.WriteLine("🧹 Child Prubsaie cleaning up");
        _guard.DiscardUnconfirmedAdds<Tiewel>();
        _guard.DiscardUnconfirmedAdds<Tiwafl>();
    }
}
